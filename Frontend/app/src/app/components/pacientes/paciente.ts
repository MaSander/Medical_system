export interface Paciente {
  id: number;
  name: string;
  cpf: string;
  dateOfBirth: string;
  idade: number;
}

export interface PacienteInfo {
  name: string;
  cpf: string;
  dateOfBirth: string;
}